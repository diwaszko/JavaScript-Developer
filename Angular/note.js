// ng
// generate - generuje coś
// build - stworzenie builda do wstawienia na strone (ng build lub ng build --prod) - prod to wersja produkcyjna, w której ciężko coś zmienić
// serve - odpalenie serwera lokalnie z obserwacja
// new - tworzy aplikacje w angularze
// bez routingu - routing (pozwala na powiązanie adresu z kompomentami z adresu url?)

// dodatki do angulara:
// Angular Snippets (Version 8)
// Angular Language Service

// Bracket Pair Colorizer 2 - koloruje tagi
// Angular console - dodatek do visual studio code - pozwala na zarządzanie angularem bez konsoli

// e2e - folder do automatycnych testów (na razie nie potrzebny)
// node_modules - folder z gotowymi modułami
// plik angular.json - najważniejszy plik z konfiguracją
// package.json - pakiety wykorzystywane w aplikacji
// folder src - kod źródłowy aplikacji
// index.html - plik w którym wszystko się wyświetla
// src/app/app.compontent.html - usunąć treść
// app.component.ts - tu się tworzy komponenty

// obiekt klasy będzie istnieć w pamięci do póki strona istnieje

// ng g --help          - wyklikać można
// ng g c counter       - tworzy komponenty / updatetuje konfiguracja (tworzy folder counter)

// devOps - jeżeli sięzaznaczy jakiś element i w konsoli napisze $0 to będzie wyświetlony zaznaczony element w DOM
